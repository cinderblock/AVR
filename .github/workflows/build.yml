name: Build

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # atmega32u4 is the library's canonical target. Other MCUs would
        # require per-MCU Ports specializations and a user-supplied
        # BDShotConfig — out of scope for header-parse / sanity CI.
        mcu: [atmega32u4]
    steps:
      - uses: actions/checkout@v7

      - name: Install AVR toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends gcc-avr avr-libc binutils-avr make

      - name: Toolchain versions
        run: |
          avr-gcc --version | head -1
          avr-g++ --version | head -1
          dpkg -s avr-libc | grep '^Version'
          make --version | head -1

      - name: Compile each .cpp standalone (-c, mmcu=${{ matrix.mcu }})
        run: |
          set -e
          shopt -s nullglob
          for f in AVR++/*.cpp; do
            # BDShot.cpp is not a standalone translation unit — it's
            # `#include`d by the consumer (along with an explicit template
            # instantiation, per the README). Exercised separately below
            # via tests/bdshot_compile_test.cpp.
            case "$f" in
              AVR++/BDShot.cpp) echo "::notice::skipping $f (intended to be included, not compiled standalone)"; continue;;
            esac
            echo "::group::$f"
            avr-g++ -c "$f" -o /dev/null \
              -mmcu=${{ matrix.mcu }} \
              -std=gnu++17 \
              -fno-exceptions \
              -funsigned-char \
              -funsigned-bitfields \
              -fpack-struct \
              -fshort-enums \
              -Os \
              -I . \
              -DF_CPU=16000000UL \
              -Wall
            echo "::endgroup::"
          done

      - name: Compile aggregate header check
        run: |
          avr-g++ -c tests/all_headers.cpp -o /dev/null \
            -mmcu=${{ matrix.mcu }} \
            -std=gnu++17 \
            -fno-exceptions \
            -funsigned-char \
            -funsigned-bitfields \
            -fpack-struct \
            -fshort-enums \
            -Os \
            -I . \
            -DF_CPU=16000000UL \
            -Wall

      - name: Compile BDShot test (user-facing #include <AVR++/BDShot.cpp> pattern)
        run: |
          avr-g++ -c tests/bdshot_compile_test.cpp -o /dev/null \
            -mmcu=${{ matrix.mcu }} \
            -std=gnu++17 \
            -fno-exceptions \
            -funsigned-char \
            -funsigned-bitfields \
            -fpack-struct \
            -fshort-enums \
            -Os \
            -I . \
            -DF_CPU=16000000UL \
            -Wall
